# Copy to .env.local (or rely on .env.development) and restart `npm run dev`.
# Without this the workspace uses the local mock agent for uploaded scenes.
NEXT_PUBLIC_SATQUERY_API=http://localhost:8000
