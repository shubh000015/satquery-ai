# SatQuery ML — two-stage land-cover pipeline.
# Most of this is preinstalled on Kaggle; running the install is still safe.

# Stage 1: the land-cover classifier we train.
torch>=2.4
torchvision>=0.19
pillow>=10.0
numpy>=1.26

# Stage 2: Qwen2.5 phrases the classifier's findings.
transformers>=4.51
accelerate>=1.0
bitsandbytes>=0.45        # 4-bit weights for the LLM; NVIDIA CUDA only

# Serving both stages to the SatQuery backend.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8

# Building the dataset from raw BigEarthNet needs pyarrow as well —
# see dataset_builder/requirements.txt (CPU-only, no GPU packages).
